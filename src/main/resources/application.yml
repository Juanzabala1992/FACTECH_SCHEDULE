spring:
    datasource:
      url: jdbc:postgresql://localhost:5432/authentication
      username: postgres
      password: admin
      driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
          hibernate:
            format_sql: true
      database: postgresql
      database-plataform: org.hibernate.dialect.PostgreSQLDialect

    main:
      allow-bean-definition-overriding: true

    mail:
      host: smp.gmail.com
      port: 587
      username: factech4dm1n@gmail.com
      password: llqvijrhhhrtqnif
      properties:
        mail:
          smtp:
            auth: true
          starttls:
            enable: true


server:
  port: 8091
  address: localhost

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG